export class Category {
    
    id: number;
    title: string;
    icon: string;

    constructor(id, title, icon) {
        this.id = id;
        this.title = title;
        this.icon= icon;
    }
    
}